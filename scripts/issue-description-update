#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
COMMAND_NAME="$(basename "$0")"

exec python3 "$REPO_ROOT/scripts/shared/command-runner.py" --repo-root "$REPO_ROOT" "$COMMAND_NAME" "$@"
